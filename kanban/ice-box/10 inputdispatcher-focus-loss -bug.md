# Cancel chord prefixes when window focus is lost

**Priority:** MEDIUM
**Type:** bug
**Raised by:** Claude (best tests #9)
**Model:** claude-sonnet-4-6

---

## Problem

When a chord prefix key is pressed (e.g., `<leader>`), the `InputDispatcher` enters a pending-prefix state. If the app loses focus before the completing key arrives, the prefix state should be cleared on focus loss so the next key after refocus is treated as a fresh input — not as the completion of the stale chord. The existing `inputdispatcher-prefix-stuck-test` covers some cases but not this focus-loss scenario.

---

## Steps

- [ ] Read `app/input_dispatcher.cpp` and the SDL window event path; define and implement focus-loss cancellation.
- [ ] Read `tests/inputdispatcher_prefix_stuck_test.cpp` (or equivalent) to understand the existing test structure.
- [ ] Add test case:
  1. Create an `InputDispatcher` with a two-key chord binding.
  2. Send the prefix key — dispatcher enters pending state.
  3. Send a "focus lost" event (SDL `SDL_EVENT_WINDOW_FOCUS_LOST` translated via the appropriate path, or call the dispatcher's focus-loss method directly).
  4. Regain focus.
  5. Send the completing key — assert it is dispatched as a standalone key event, NOT as a chord completion.
- [ ] Add a second case: prefix pressed, focus lost, focus regained, prefix pressed again, completing key pressed — assert chord fires correctly.
- [ ] Run `cmake --build build --target draxul-tests && ctest`.
- [ ] Run focused input tests and same-cache smoke; formatting remains hook-owned.

---

## Acceptance Criteria

- [ ] Focus-loss clears pending prefix state.
- [ ] After refocus a fresh chord can be started and completed normally.

*Work item generated by claude-sonnet-4-6*
