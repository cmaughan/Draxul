---
# Agent Scripts Deduplication

## Summary
`ask_agent_gpt.py`, `ask_agent_claude.py`, and `ask_agent_gemini.py` duplicate `resolve_path`, prompt-file loading, prepend-file handling, and output handling logic. Will drift silently.

## Resolution

Superseded by the shared `draxul-review` runner. The duplicated scripts were
removed; provider differences now live in a single adapter table and command
builder.

## Context
`scripts/ask_agent_gpt.py`, `scripts/ask_agent_claude.py`, `scripts/ask_agent_gemini.py`. The three scripts evolved in parallel and now share substantial boilerplate. Any fix to the shared logic (e.g., path resolution on Windows) must be applied three times.

## Steps
- [x] Catalogue the duplicated behavior.
- [x] Replace it with one shared review runner and provider adapter table.
- [x] Remove the superseded wrappers.
- [x] Validate the replacement command surface and adapters.

## Acceptance Criteria
- [x] Shared loading, output, validation, and command logic has one implementation.
- [x] Provider-specific behavior is declarative and minimal.
- [x] Repo review usage is documented through the replacement skills.

## Notes
None.

*Work item generated by claude-sonnet-4-6*
